/*
 * watch.c
 *
 *  Created on: 9 нояб. 2023 г.
 *      Author: aasmr
 */

#include "watch.h"
/*
static void transf(float* new_x, float* new_y, uint8_t x, uint8_t y, uint8_t xc, uint8_t yc, uint16_t angle)
{
    int8_t coef = 1;
    if(angle < 0)
    {
        coef = -1;
        angle = -1 * angle;
    }

    float ft_x, ft_y, st_x, st_y, tt_x, tt_y;

    ft_x = (x - xc) - (coef*(bradis_tan[angle]/10000)*(y - yc));
    ft_y = (y - yc);

    st_x = ft_x;
    st_y = ft_x * (coef*bradis_sin[angle]/10000) + ft_y;

    tt_x = st_x - (coef*(bradis_tan[angle]/10000) * st_y);
    tt_y = st_y

    *new_x = tt_x + xc;
    *new_y = tt_y + yc;
}

static void rotate()
{

}
*/
